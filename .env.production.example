# ── Production Environment ───────────────────────────────────────────────────
# In production, inject these as real environment variables (container secrets,
# Kubernetes Secrets, AWS SSM, etc.) — do NOT commit a populated .env.production.
NODE_ENV=production

# ── PostgreSQL ───────────────────────────────────────────────────────────────
# PGHOST is the managed DB host (RDS endpoint, Cloud SQL, etc.), not localhost.
PGHOST=
PGPORT=5432
PGUSER=kalastra
PGDATABASE=pokemonrs
PGPASSWORD=

# Full URL must be set; PG* vars are a fallback only for local tooling.
DATABASE_URL=postgres://kalastra:CHANGEME@your-db-host:5432/pokemonrs

# ── Backend ──────────────────────────────────────────────────────────────────
BIND_ADDR=0.0.0.0:8080
RUST_LOG=warn
# Required in production — comma-separated bearer tokens.
API_KEYS=CHANGEME_TOKEN1,CHANGEME_TOKEN2

# ── RL / Gameplay ─────────────────────────────────────────────────────────────
COST_PER_SPIN=0.01
HUMAN_LIKENESS_WEIGHT=0.3
RATE_LIMIT_RPM=60

# ── Frontend (Vite) ───────────────────────────────────────────────────────────
# Relative path so nginx proxies /v1 → api service internally.
VITE_API_BASE_URL=/v1
