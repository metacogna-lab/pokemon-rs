# ── Environment ──────────────────────────────────────────────────────────────
# Copy this file to .env and fill in secrets. .env is gitignored.
# NODE_ENV controls dev-mode defaults (e.g. PGHOST=localhost, open API_KEYS).
NODE_ENV=dev

# ── PostgreSQL ───────────────────────────────────────────────────────────────
# Individual vars for psql/pgcli/migration scripts.
# NODE_ENV=dev: PGHOST=localhost, PGPORT=5432 (standard Postgres port).
PGHOST=localhost
PGPORT=5432
PGUSER=kalastra
PGDATABASE=pokemonrs
PGPASSWORD=

# Assembled connection URL (sqlx reads this; PG* vars are a fallback if unset).
DATABASE_URL=postgres://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}

# ── Backend ──────────────────────────────────────────────────────────────────
BIND_ADDR=0.0.0.0:8080
RUST_LOG=info
# Comma-separated bearer tokens. Empty = dev mode (accepts any non-empty token).
API_KEYS=

# ── RL / Gameplay ─────────────────────────────────────────────────────────────
COST_PER_SPIN=0.01
HUMAN_LIKENESS_WEIGHT=0.3
RATE_LIMIT_RPM=100

# ── Frontend (Vite) ───────────────────────────────────────────────────────────
VITE_API_BASE_URL=http://localhost:8080/v1
